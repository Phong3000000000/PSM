<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Inherit stock.picking form view to add complaint smart button -->
        <record id="view_picking_form_inherit_complaint" model="ir.ui.view">
            <field name="name">stock.picking.form.complaint</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@name='button_box']" position="inside">
                    <button name="action_view_complaint" type="object" 
                            class="oe_stat_button" icon="fa-exclamation-triangle"
                            invisible="not complaint_id">
                        <div class="o_field_widget o_stat_info">
                            <span class="o_stat_text">Complaint</span>
                        </div>
                    </button>
                </xpath>
                
                <!-- Add complaint_id field in header for visibility (invisible) -->
                <xpath expr="//field[@name='state']" position="before">
                    <field name="complaint_id" invisible="1"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
