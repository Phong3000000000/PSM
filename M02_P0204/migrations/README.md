# Migration History Note

## Version Marker Normalization

- Previous release marker: `1.0.1`
- Current normalized release marker: `1.1`

## Historical Migration Policy

The existing folder `migrations/1.0.1` is historical upgrade logic for prior releases.
It remains part of module history and must be preserved.

Do not delete or rename `migrations/1.0.1` unless the team explicitly decides to rewrite migration policy.
