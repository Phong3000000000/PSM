<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data noupdate="1">

    <!-- Example:
    <record id="approval_mail_template_request" model="mail.template">
      <field name="name">Approval Request</field>
      <field name="model_id" ref="base.model_approval_request"/>
      <field name="subject">Approval Request: ${object.display_name}</field>
      <field name="email_from">${(user.email_formatted or '')|safe}</field>
      <field name="email_to">${(object.requested_by_id.email or '')|safe}</field>
      <field name="body_html" type="html">
        <![CDATA[
          <p>Hello,</p>
          <p>Please approve: <strong>${object.display_name}</strong></p>
        ]]>
      </field>
    </record>
    -->

  </data>
</odoo>
